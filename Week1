# 1. Write a program that takes a word as an input and print the number of vowels in the word.
def count_vowels(word):
    vowels="aeiouAEIOU"
    count=0
    for char in word:
        if char in vowels:
            count += 1
    return count

input_word = input("Please enter a word: ")
print("The number of vowels is:", count_vowels(input_word))


# 2. Iterate through the following list of animals and print each one in all caps. animals=['tiger', 'elephant', 'monkey', 'zebra', 'panther']
animals=['tiger', 'elephant', 'monkey', 'zebra', 'panther']
for animal in animals:
    print(animal.upper())

# 3. Write a program that iterates from 1 to 20, printing each number and whether it's odd or even.
def odd_even(n):
    if n % 2 == 0:
        return "even"
    else:
        return "odd"

for i in range(1, 21):
    print(i, "is an", odd_even(i))

# 4. Write a program to check if a string is a palindrome or not.
input_string = input("Please enter a string: ")

def is_palindrome(s):
    for p in string.punctuation:
        s = s.replace(p, "")
    s = s.replace(" ", "").lower()
    return s == s[::-1]
if is_palindrome(input_string):
    print(f'"{input_string}" is a palindrome.') 
else:
    print(f'"{input_string}" is not a palindrome.')

# 5. Write a function sum_of_integers(a, b) that takes two integers as input from the user and returns their sum.
print('-'*50)
def sum_of_integers(a, b):
    return a + b
a = int(input("Enter the first integer: "))
b = int(input("Enter the second integer: "))
print("The sum ot the above tow intergers is:", sum_of_integers(a, b))



